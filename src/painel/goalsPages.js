import { searchGoals } from "../services/goals.js";

function html(body, title) {
  return `<!DOCTYPE html><html lang="pt-BR"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="theme-color" content="#090a0d"><title>${title}</title><link rel="preconnect" href="https://fonts.googleapis.com"><link rel="preconnect" href="https://fonts.gstatic.com" crossorigin><link href="https://fonts.googleapis.com/css2?family=Barlow+Condensed:wght@700;800;900&family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet"><style>
  :root{--bg:#090a0d;--card:#11141a;--card2:#171b23;--input:#08090c;--line:rgba(255,255,255,.09);--line-red:rgba(239,68,68,.28);--red:#e3262e;--red2:#a70d16;--gold:#f2c94c;--text:#f8fafc;--muted:#98a2b3;--muted2:#cbd5e1;--shadow:0 24px 70px rgba(0,0,0,.35)}
  *{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;min-height:100vh;background:radial-gradient(circle at 15% 0%,rgba(227,38,46,.14),transparent 26%),radial-gradient(circle at 90% 18%,rgba(227,38,46,.06),transparent 26%),var(--bg);color:var(--text);font-family:Inter,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;padding:20px 14px 40px}a{color:inherit}.wrap{max-width:880px;margin:auto;background:linear-gradient(180deg,rgba(255,255,255,.015),transparent 22%),var(--card);border:1px solid var(--line);border-top:3px solid var(--red);border-radius:24px;padding:26px;box-shadow:var(--shadow)}.head{display:flex;justify-content:space-between;gap:18px;align-items:flex-start;flex-wrap:wrap;padding-bottom:18px;border-bottom:1px solid var(--line);margin-bottom:18px}.head h1{margin:0;font-family:"Barlow Condensed",Inter,sans-serif;font-size:clamp(34px,7vw,52px);line-height:.95;letter-spacing:-.02em;text-transform:uppercase;font-weight:900}.head h1::first-letter{color:var(--red)}.muted{color:var(--muted);font-size:13px;line-height:1.55}.head .muted{margin-top:10px;font-size:14px}.nav{display:flex;gap:9px;flex-wrap:wrap}.btn,.nav a{border:1px solid var(--line);border-radius:12px;padding:12px 16px;font-weight:800;text-decoration:none;cursor:pointer;color:#fff;transition:transform .15s ease,border-color .15s ease,background .15s ease;box-shadow:none}.btn:active,.nav a:active{transform:scale(.98)}.nav a,.btn-secondary{background:#202838;color:#fff}.nav a:hover,.btn-secondary:hover{border-color:rgba(255,255,255,.18);background:#263147}.btn-primary{background:linear-gradient(135deg,var(--red),var(--red2));color:#fff;border-color:rgba(255,255,255,.08);box-shadow:0 10px 26px rgba(227,38,46,.22)}.btn-danger{background:#7f1d1d;color:#fff;border-color:#991b1b}.grid{display:grid;grid-template-columns:1fr 1fr;gap:14px}.field{display:flex;flex-direction:column;gap:8px;margin:13px 0}label{font-size:12px;color:var(--muted2);font-weight:800;text-transform:uppercase;letter-spacing:.055em}input{width:100%;padding:15px 16px;border-radius:13px;border:1px solid var(--line);background:var(--input);color:#fff;font:600 15px/1.2 Inter,sans-serif;outline:none;transition:border-color .15s ease,box-shadow .15s ease,background .15s ease}input::placeholder{color:#5f6673;font-weight:500}input:focus{border-color:rgba(227,38,46,.75);box-shadow:0 0 0 3px rgba(227,38,46,.12);background:#0b0d11}.alert{display:none;padding:13px 15px;border-radius:12px;margin:14px 0;color:#fff;font-weight:700;border:1px solid rgba(255,255,255,.08)}.searchrow{display:grid;grid-template-columns:1fr auto;gap:12px;align-items:end;margin:16px 0 22px;padding:15px;background:var(--card2);border:1px solid var(--line);border-radius:16px}.searchrow .field{margin:0}.searchrow .btn{min-height:48px}.searchrow>input{align-self:end}.btn-primary[type="submit"]{width:100%;margin-top:8px;padding:15px 18px;font-size:15px;letter-spacing:.02em}table{width:100%;border-collapse:separate;border-spacing:0 8px;margin-top:14px}th,td{padding:13px 12px;text-align:left}th{color:var(--muted2);font-size:11px;text-transform:uppercase;letter-spacing:.06em;border-bottom:1px solid var(--line)}tbody tr{background:var(--card2)}tbody td{border-top:1px solid var(--line);border-bottom:1px solid var(--line)}tbody td:first-child{border-left:1px solid var(--line);border-radius:12px 0 0 12px}tbody td:last-child{border-right:1px solid var(--line);border-radius:0 12px 12px 0}code{color:var(--gold);font-weight:700}.actions{display:flex;gap:7px;flex-wrap:wrap}.actions .btn{padding:9px 11px;font-size:12px}.wrap>form{padding-top:2px}.wrap>form .field{position:relative}.wrap>form label:before{content:"";display:inline-block;width:4px;height:4px;border-radius:50%;background:var(--red);vertical-align:middle;margin-right:7px;margin-bottom:2px}.head .nav a:first-child{border-color:rgba(242,201,76,.25)}.head .nav a:last-child{border-color:var(--line-red)}
  @media(max-width:640px){body{padding:14px 10px 34px}.wrap{padding:20px 16px;border-radius:22px}.head{gap:14px}.head h1{font-size:42px}.nav{width:100%}.nav a{flex:1;text-align:center}.grid{grid-template-columns:1fr}.searchrow{grid-template-columns:1fr;padding:12px}.searchrow .btn{width:100%}table{font-size:13px}.hide-mobile{display:none}.actions{flex-direction:column}.actions .btn{width:100%}}
  @media(max-width:390px){.head h1{font-size:38px}.wrap{padding:18px 14px}.btn,.nav a{padding:11px 12px}input{padding:14px}}
  </style></head><body>${body}</body></html>`;
}

export async function processarGolsPages(request, env) {
  const url = new URL(request.url);

  if (url.pathname === "/api/painel-addgoal" && request.method === "GET") {
    const body = `<div class="wrap"><div class="head"><div><h1 id="panelTitle">⚽ Adicionar Novo Gol</h1><div class="muted" id="panelSubtitle">Preencha os campos para salvar no D1.</div></div><div class="nav"><a href="/api/lista-gols">📋 Acervo</a><a href="/api/painel-bolao">🏆 Bolão</a></div></div>
    <div id="alertBox" class="alert"></div>
    <div class="searchrow"><div class="field"><label>ID do gol para edição</label><input id="goal_id" placeholder="Ex: 1718362402941"></div><button class="btn btn-secondary" onclick="buscarGol()">🔍 Buscar</button></div>
    <form id="goalForm"><div class="field"><label>Confronto / Jogo</label><input id="jogo" required placeholder="Flamengo 2x1 Vasco"></div><div class="grid"><div class="field"><label>Autor</label><input id="autor" required></div><div class="field"><label>Assistência</label><input id="assistencia" required></div></div><div class="grid"><div class="field"><label>Campeonato</label><input id="campeonato" required></div><div class="field"><label>Fase / Rodada</label><input id="fase" required></div></div><div class="field"><label>Telegram FileID do vídeo</label><input id="file_id" required></div><div class="field"><label>ID Administrador</label><input type="password" id="admin_id" value="7717528550" required></div><button class="btn btn-primary" type="submit">💾 Salvar Gol</button></form></div>
    <script>
    const id=document.getElementById('goal_id'), box=document.getElementById('alertBox');
    const qs=new URLSearchParams(location.search); if(qs.get('edit_id')){id.value=qs.get('edit_id');buscarGol();}
    function aviso(txt,ok){box.style.display='block';box.style.background=ok?'#065f46':'#991b1b';box.textContent=txt}
    async function buscarGol(){if(!id.value.trim())return aviso('Digite um ID válido.',false);const r=await fetch('/api/getgoal?id='+encodeURIComponent(id.value.trim()));const d=await r.json();if(!d.ok)return aviso(d.error||'Gol não encontrado',false);for(const k of ['jogo','autor','assistencia','campeonato','fase','file_id'])document.getElementById(k).value=d.gol[k]||'';document.getElementById('panelTitle').textContent='📝 Editar Gol Existente';aviso('Dados carregados.',true)}
    document.getElementById('goalForm').addEventListener('submit',async e=>{e.preventDefault();const payload={id:id.value.trim(),jogo:jogo.value,autor:autor.value,assistencia:assistencia.value,campeonato:campeonato.value,fase:fase.value,file_id:file_id.value,admin_id:admin_id.value};if(!confirm('Confirma salvar este gol?'))return;const r=await fetch('/api/addgoal-action',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const d=await r.json();if(d.ok){aviso('✅ Gol salvo com ID '+d.id,true);id.value='';e.target.reset();admin_id.value='7717528550'}else aviso('❌ '+(d.error||'Erro ao salvar'),false)});
    </script>`;
    return new Response(html(body, "Painel de Gols"), { headers: { "Content-Type": "text/html; charset=utf-8", "Cache-Control": "no-store" } });
  }

  if (url.pathname === "/api/lista-gols" && request.method === "GET") {
    const q = String(url.searchParams.get("q") || "").trim();
    let gols;
    if (q) {
      gols = await searchGoals(env.DB, q, { limit: 500, offset: 0 });
    } else {
      const { results = [] } = await env.DB.prepare(`SELECT * FROM gols ORDER BY CAST(criado_em AS INTEGER) DESC, CAST(id AS INTEGER) DESC LIMIT 30`).all();
      gols = results;
    }
    const totalRow = await env.DB.prepare("SELECT COUNT(*) AS total FROM gols").first();
    const total = q ? gols.length : (totalRow?.total || 0);
    const rows = gols.map(g => `<tr id="row_${g.id}"><td><code>${g.id}</code></td><td><strong>${g.jogo||'Jogo'}</strong><div class="muted">🏆 ${g.campeonato||'-'} | ${g.fase||'-'}</div></td><td>⚽ ${g.autor||'-'}<div class="muted">🅰 ${g.assistencia||'-'}</div></td><td><div class="actions"><a class="btn btn-secondary" href="/api/painel-addgoal?edit_id=${encodeURIComponent(g.id)}">📝 Editar</a><button class="btn btn-danger" onclick="apagar('${String(g.id).replace(/'/g,"\\'")}')">🗑️ Apagar</button></div></td></tr>`).join('');
    const body = `<div class="wrap"><div class="head"><h1>📋 Gols (${total})</h1><div class="nav"><a href="/api/painel-addgoal">➕ Novo</a><a href="/api/painel-bolao">🏆 Bolão</a></div></div><div class="searchrow"><input id="q" value="${q.replace(/"/g,'&quot;')}" placeholder="Buscar jogador, jogo, campeonato..."><button class="btn btn-secondary" onclick="buscar()">🔍 Buscar</button></div>${gols.length?`<div style="overflow:auto"><table><thead><tr><th>ID</th><th>Jogo</th><th>Gol</th><th>Ações</th></tr></thead><tbody>${rows}</tbody></table></div>`:'<p class="muted">Nenhum gol encontrado.</p>'}${!q?'<p class="muted">Exibindo os 30 gols mais recentes.</p>':''}</div><script>function buscar(){const v=document.getElementById('q').value.trim();location.href='/api/lista-gols'+(v?'?q='+encodeURIComponent(v):'')}document.getElementById('q').addEventListener('keydown',e=>{if(e.key==='Enter')buscar()});async function apagar(id){if(!confirm('Excluir este gol permanentemente?'))return;const r=await fetch('/api/deletegoal?id='+encodeURIComponent(id),{method:'DELETE'});const d=await r.json();if(d.ok){document.getElementById('row_'+id)?.remove()}else alert(d.error||'Erro ao excluir')}</script>`;
    return new Response(html(body, "Acervo de Gols"), { headers: { "Content-Type": "text/html; charset=utf-8", "Cache-Control": "no-store" } });
  }

  return null;
}
